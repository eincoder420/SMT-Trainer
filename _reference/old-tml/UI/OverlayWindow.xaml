<Window x:Class="TooMuchLightTrainer.UI.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TML Trainer Overlay"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        IsHitTestVisible="False"
        WindowStartupLocation="Manual"
        Left="0" Top="0">

    <Grid x:Name="RootGrid">
        <!-- Animated particle/scanline overlay (decorative) -->
        <Canvas x:Name="ParticleCanvas" IsHitTestVisible="False" Opacity="0.08">
            <!-- Filled by code-behind with animated dots -->
        </Canvas>

        <!-- Aimbot ESP Canvas -->
        <Canvas x:Name="EspCanvas" IsHitTestVisible="False" />

        <!-- Menu container (positioned top-left) -->
        <Canvas x:Name="MenuCanvas"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Margin="24,24,0,0"
                IsHitTestVisible="False"/>



        <!-- Dynamic Animated Top-Right Notifications Host -->
        <StackPanel x:Name="NotificationHost"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Margin="0,52,16,0"
                    IsHitTestVisible="False"/>


    </Grid>
</Window>
